export interface MonthlyRecord {
    id: number;

    userId: number;

    yearId: number;

    month: number;

    grossSalary: number;

    netSalary: number;

    createdAt: string;

    updatedAt: string;
}
